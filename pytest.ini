[pytest]
addopts = -q
testpaths = tests
norecursedirs = third_party .venv
markers =
    integration: tests that need running services
    overspeed: overspeed integration tests (core + io)
    extension: project extension validation (NX1)